<?php
session_start();

if (!isset($_SESSION['saldo'])) {
    $_SESSION['saldo'] = 0;
}

$mensagem = "";

if ($_SERVER["REQUEST_METHOD"] == "POST") {

    $acao = $_POST['acao'];
    $valor = isset($_POST['valor']) ? floatval($_POST['valor']) : 0;

    if ($acao == "depositar") {
        if ($valor > 0) {
            $_SESSION['saldo'] += $valor;
            $mensagem = "Depósito realizado com sucesso.";
        } else {
            $mensagem = "Valor inválido para depósito.";
        }
    }

    if ($acao == "sacar") {
        if ($valor <= 0) {
            $mensagem = "Valor inválido para saque.";
        } elseif ($valor > $_SESSION['saldo']) {
            $mensagem = "Saldo insuficiente para realizar o saque.";
        } else {
            $_SESSION['saldo'] -= $valor;
            $mensagem = "Saque realizado com sucesso.";
        }
    }

    if ($acao == "extrato") {
        $mensagem = "Extrato exibido abaixo.";
    }
}
?>

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Caixa Eletrônico</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #ede7f6;
        }

        .caixa {
            width: 300px;
            margin: 100px auto;
            padding: 20px;
            background-color: #ffffff;
            border: 1px solid #b39ddb;
            text-align: center;
            border-radius: 6px;
        }

        h2 {
            color: #5e35b1;
        }

        input, button {
            width: 100%;
            padding: 8px;
            margin-top: 8px;
            box-sizing: border-box;
        }

        input {
            text-align: center;
            border: 1px solid #b39ddb;
        }

        button {
            background-color: #9575cd;
            border: none;
            color: #ffffff;
            cursor: pointer;
            font-weight: bold;
        }

        button:hover {
            background-color: #7e57c2;
        }

        .mensagem {
            margin-top: 15px;
            font-weight: bold;
            color: #5e35b1;
        }

        .saldo {
            margin-top: 10px;
            font-weight: bold;
            color: #4527a0;
        }
    </style>
</head>
<body>

<div class="caixa">
    <h2>Caixa Eletrônico</h2>

    <form method="post">
        <input type="number" name="valor" step="0.01" placeholder="Informe o valor">

        <button type="submit" name="acao" value="depositar">Depositar</button>
        <button type="submit" name="acao" value="sacar">Sacar</button>
        <button type="submit" name="acao" value="extrato">Extrato</button>
    </form>

    <div class="mensagem">
        <?php echo $mensagem; ?>
    </div>

    <div class="saldo">
        Saldo atual: R$ <?php echo number_format($_SESSION['saldo'], 2, ',', '.'); ?>
    </div>
</div>

</body>
</html>
